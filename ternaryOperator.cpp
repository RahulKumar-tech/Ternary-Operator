#include<iostream>
using namespace std;
int main(){
    int n;
    cin>>n;
    // if(n%2==0){
    //     cout<<"even";
    // }
    // else{
    //     cout<<"odd";
    // }

    // it can be also writeen as because of single line statement
    // if(n%2==0) cout<<"even";
    // else cout<<"odd";
    
    //condition ? true : false
    // means condition agar true agar false
      (n%2==0) ? cout<<"even" : cout<<"odd";
   }